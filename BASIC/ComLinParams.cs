using System;

namespace BASIC
{
    class ComLinParams
    {
        static void Main(string[] args)
        {
            foreach (string str in args)
                Console.WriteLine(str);
        }
    }
}
